<?xml version="1.0" encoding="utf-8"?>
<!--
  Светлый вариант. Тёмный лежит в values-night/themes.xml.

  Раньше здесь был ОДИН стиль со светлым родителем и почти чёрным
  windowBackground (#0F0E12) — рассогласование, из-за которого кадр между
  стартом окна и первой композицией показывался не того цвета.
-->
<resources>
    <style name="Theme.AnilibrixPlus" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <!-- Стартовая тема: до неё холодный старт показывал голый windowBackground. -->
    <style name="Theme.AnilibrixPlus.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.AnilibrixPlus</item>
    </style>
</resources>
